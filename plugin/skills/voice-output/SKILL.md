---
name: voice-output
description: Озвучивает ответ Claude голосом через MCP claude-tts. БЛОКИРУЮЩЕЕ требование — вызывай mcp__claude-tts__speak ПЕРВЫМ tool-действием в ответе, ДО основного текста (голос идёт ВПЕРЕДИ текста, НЕ в конце), на любом языке, любой длины, по любой теме. Триггер — буквально любое сообщение пользователя. Пропуск speak ИЛИ вызов его в конце вместо начала считается багом.
---

# Voice Output — обязательная озвучка ответа

## Главное правило (НЕ ОПЦИОНАЛЬНО)

**Озвучка идёт ПЕРВОЙ, до основного текста ответа.** Порядок в каждом ответе:

1. Сформулируй финальный ответ пользователю в голове целиком.
2. **Первым tool-вызовом** в ответе зови `mcp__claude-tts__speak` с этим текстом, очищенным от непроизносимого (см. ниже). Звук пойдёт, пока пишется текст.
3. Затем пиши тот же самый текст в чат (с markdown, кодом, путями — как обычно для глаза).

**КРИТИЧНО: текст в `speak` и текст в чате — один и тот же ответ, слово в слово.** Не сочиняй для голоса отдельный вариант. Разница только в том, что для голоса убрано непроизносимое.

## Что озвучивается, а что нет

Озвучивается только то, что пользователь видит целиком:
- **финальный ответ** (текст после последнего вызова инструментов);
- **сообщения пользователю по ходу работы** (например, через send_user_message: «нужно твоё участие…») — у каждого свой `speak`.

**Промежуточные пояснения между вызовами инструментов НЕ озвучиваются** («Проверяю…», «Читаю файл…»): интерфейс показывает их свёрнутыми в шаги, вслух они звучат как служебный шум.

## Очередь — речь никогда не перебивается

Каждый вызов `speak` встаёт в очередь после того, что уже звучит (включая ответы из других чатов). Обычный вызов = НОВОЕ сообщение: если что-то ещё звучит, движок сам объявит его паузой и фразой «Следующее сообщение». `queue=true` ставь ТОЛЬКО когда продолжаешь тот же самый ответ частями — тогда объявления не будет.

## Легальные основания пропустить `speak`

1. Озвучка в этом чате выключена командой-переключателем **`/voice`** (скилл `voice`): нечётное число вызовов `/voice` в чате = выключена, чётное = включена. Пока выключена — не зови `speak`. Автоозвучка `tts-watch` видит ту же команду и тоже молчит. Обычные слова («тихо», «без озвучки») озвучку НЕ выключают — только команда.
2. Очищенный текст оказался пустым (нечего читать).
3. Предыдущий `speak` в этой сессии вернул ошибку — пропусти ОДИН раз, потом снова пробуй.

Без исключений на основании «техническая отладка», «много кода», «ответ короткий», «не просили голос явно».

## Как очищать текст для голоса

1. **Markdown-символы** убрать, СЛОВА оставить: `**жирный**`→жирный, `# Заголовок`→Заголовок, `[текст](url)`→текст, бэктики убрать.
2. **Блоки в рамке (между ```)** — смотри, ЧТО внутри:
   - **код, команды, конфиги, пути, JSON** — не читать;
   - **обычная человеческая речь** — готовый текст для отправки, объявление, сообщение, цитата, формулировка — **ЧИТАТЬ дословно**. Это часто самое важное в ответе: пользователь должен услышать, что именно отправит.
3. **Пути, URL, голые домены, `computer://`, длинные ID и хеши** — удалить.
4. **Служебная «кухня» интерфейса** — не читать: «Used … integration», «(3 actions) · 4 notes», имена инструментов `mcp__…`, блок «Источники/Sources» в конце ответа целиком.
5. **Таблиц в озвучиваемых ответах избегай** — излагай прозой одинаково в чате и голосом. Если таблица всё же есть, вслух её не читай.
6. **Списки** — читай пункты теми же словами, по порядку.

## Язык озвучки — язык пользователя

Если кусок ответа на языке, который пользователь на слух не воспринимает (обычно английский при русском общении):
- иноязычные цитаты, готовые тексты, сообщения об ошибках — для голоса **переведи**, в чат пиши оригинал, если он нужен для копирования;
- если перевод бессмыслен (листинг команд, конфиг) — замени одной фразой по-русски: «дальше идёт готовый текст на английском»;
- отдельные термины (`commit`, `pull request`) оставляй как есть.

## Длина — полностью, без сокращений

Голос читает ВЕСЬ ответ после очистки, независимо от длины. Не резюмируй и не сокращай. Если ответ слишком длинный для прослушивания — короче должен быть сам текст ответа.

## Команды управления голосом

| Пользователь сказал | Tool |
|---|---|
| «переключи на Свету» / «Светлану» | `set_voice` name=Svetlana |
| «переключи на Ирину» | `set_voice` name=Irina |
| «быстрее» / «медленнее» | `set_rate` ±1 (текущее — через `get_state`) |
| «громче» / «тише» (про громкость) | `set_volume` ±10 |
| «покажи голоса» | `list_voices` |
| «стоп» / «хватит читать» / «замолчи» | `stop` (и НЕ зови `speak` в этом ответе) |
| «пауза» / «продолжай» | `pause` / `resume` |

## Как пользователю управлять озвучкой (расскажи, если спросит или если озвучка ему мешает)

- **`/voice`** — переключатель: выключить / снова включить озвучку в ЭТОМ чате (и голос Claude, и автоозвучку).
- **Ctrl+Alt+A** — звук озвучки вкл/выкл ВЕЗДЕ (помнит громкость).
- **Ctrl+Alt+плюс / минус** — громкость ±10.
- **Ctrl+Alt+C** — остановить речь и очистить очередь.
- **Ctrl+Alt+X** — пауза / продолжить. **Ctrl+Alt+R** — перезапуск озвучки, если пропал звук.
- **Ctrl+Alt+Z** — прочитать выделенный текст в любом окне (встаёт в очередь).

## Технические правила

- `speak` асинхронный — вызывай первым tool-действием, не жди завершения речи.
- Если `speak` вернул ошибку — не извиняйся, не повторяй в этом ответе.
- Не озвучивай служебный вывод инструментов: bash, JSON, диагностику.

## Самопроверка перед отправкой

1. Вызвал ли я `speak` первым tool-вызовом (или есть легальное исключение)?
2. Совпадает ли текст в `speak` с текстом в чате слово в слово?
3. Если в ответе есть блок в рамке с готовым текстом на человеческом языке — попал ли он в `speak`?
